[project]
name = "ai-band"
version = "0.1.0"
description = "REAPER-oriented AI band sketch generator"
requires-python = ">=3.11"
readme = "README.md"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]

